import React from "react";
import { View, Text } from "react-native";

const Splash = () => {
    return(
        <View style={{justifyContent: "center"}}>
            <Text style={{fontSize: 32, fontWeight: "bold"}}>OPAYN HRMS</Text>
        </View>
    );
};

export default Splash;