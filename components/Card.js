import { Image, View } from "react-native";
import React from "react";
import { CardStyle } from "../CustomStyle/CardStyle";

export const Card = () =>{
    return(
        <View style = {CardStyle.mainContainer}>
            
        </View>
    );
}